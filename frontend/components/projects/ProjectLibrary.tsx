"use client";
import {useMemo,useState} from "react";import type{ProjectLibraryItem}from"@/lib/projects";import{ProjectCard}from"./ProjectCard";
export function ProjectLibrary({projects,onStart,startingId}:{projects:ProjectLibraryItem[];onStart:(id:string)=>void;startingId:string|null}):JSX.Element{const[category,setCategory]=useState("all"),[difficulty,setDifficulty]=useState("all"),[status,setStatus]=useState("all"),[search,setSearch]=useState("");const filtered=useMemo(()=>projects.filter(p=>(category==="all"||p.category===category)&&(difficulty==="all"||p.difficulty_level===Number(difficulty))&&(status==="all"||(status==="not_started"?!p.user_status:p.user_status===status))&&`${p.title} ${p.short_description}`.toLowerCase().includes(search.toLowerCase())),[projects,category,difficulty,status,search]);return <section><div className="grid gap-3 rounded-xl border border-slate-800 bg-slate-900/50 p-4 sm:grid-cols-4"><select value={category} onChange={e=>setCategory(e.target.value)} className="rounded-lg border border-slate-700 bg-slate-950 p-2 text-sm"><option value="all">All categories</option>{Array.from(new Set(projects.map(p=>p.category))).map(item=><option key={item}>{item}</option>)}</select><select value={difficulty} onChange={e=>setDifficulty(e.target.value)} className="rounded-lg border border-slate-700 bg-slate-950 p-2 text-sm"><option value="all">All difficulties</option>{[1,2,3,4,5].map(item=><option key={item} value={item}>Difficulty {item}</option>)}</select><select value={status} onChange={e=>setStatus(e.target.value)} className="rounded-lg border border-slate-700 bg-slate-950 p-2 text-sm"><option value="all">All status</option><option value="not_started">Not started</option><option value="active">Active</option><option value="completed">Completed</option></select><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search projects" className="rounded-lg border border-slate-700 bg-slate-950 p-2 text-sm"/></div><div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{filtered.map(project=><ProjectCard key={project.id} project={project} onStart={onStart} starting={startingId===project.id}/>)}</div></section>}
