import type { Achievement } from "@/lib/progress";
export function AchievementCard({ achievement }: { achievement: Achievement }): JSX.Element { const earned=Boolean(achievement.earned_at); return <article className={`rounded-2xl border p-5 ${earned?"border-amber-400/25 bg-amber-400/5 shadow-[0_0_22px_rgba(251,191,36,.08)]":"border-slate-800 bg-slate-900/60 opacity-60"}`}><div className="flex items-start gap-4"><span className={`flex h-12 w-12 shrink-0 items-center justify-center rounded-xl text-2xl ${earned?"bg-amber-300/15":"bg-slate-800 grayscale blur-[1px]"}`}>{achievement.icon||"🏅"}</span><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><h3 className="font-bold">{achievement.name}</h3><span className="shrink-0 text-xs font-bold text-amber-300">+{achievement.xp_reward} XP</span></div><p className="mt-1 text-sm text-slate-400">{achievement.description}</p>{earned?<p className="mt-3 text-xs text-emerald-300">Earned {new Date(achievement.earned_at!).toLocaleDateString()}</p>:<div className="mt-3"><div className="h-1.5 overflow-hidden rounded bg-slate-800"><div className="h-full rounded bg-indigo-400" style={{width:`${Math.min(100,(achievement.progress??0)*100)}%`}}/></div><p className="mt-1 text-xs text-slate-500">{achievement.progress_label??"Keep learning to unlock"}</p></div>}</div></div></article> }
