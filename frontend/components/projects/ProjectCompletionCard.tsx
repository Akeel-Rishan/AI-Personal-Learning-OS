"use client";
import type { Completion } from "@/lib/projects";
export function ProjectCompletionCard({completion}:{completion:Completion}):JSX.Element{const c=completion.certificate_data;const print=()=>{document.body.classList.add("printing-certificate");window.print();window.setTimeout(()=>document.body.classList.remove("printing-certificate"),500)};return <section className="rounded-2xl border border-emerald-400/30 bg-emerald-400/5 p-6 text-center"><h1 className="text-3xl font-black text-emerald-300">Project complete! 🎉</h1><p className="mx-auto mt-3 max-w-2xl text-slate-300">{completion.completion_message}</p><div className="mt-5 grid grid-cols-3 gap-3"><div className="rounded-xl bg-slate-950/50 p-4"><b>{completion.total_stages}</b><p className="text-xs text-slate-500">Stages</p></div><div className="rounded-xl bg-slate-950/50 p-4"><b>{Math.round(completion.average_stage_score*100)}%</b><p className="text-xs text-slate-500">Score</p></div><div className="rounded-xl bg-slate-950/50 p-4"><b>+{completion.total_xp_earned}</b><p className="text-xs text-slate-500">XP</p></div></div><div className="certificate-content mx-auto mt-8 max-w-3xl border-8 border-double border-indigo-300 bg-white p-10 text-slate-900"><p className="uppercase tracking-[.35em] text-indigo-700">Certificate of Completion</p><h2 className="mt-6 text-4xl font-serif font-bold">{c.learner_name}</h2><p className="mt-4">has successfully completed</p><h3 className="mt-3 text-2xl font-bold text-indigo-800">{c.project_title}</h3><p className="mt-6">Grade {c.grade} · {c.score_percentage}% · {c.completion_date}</p></div><button onClick={print} className="mt-5 rounded-lg bg-emerald-400 px-5 py-2 font-bold text-slate-950">Print certificate</button></section>}
