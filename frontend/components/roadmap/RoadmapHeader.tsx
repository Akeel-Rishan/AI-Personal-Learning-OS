import type { Roadmap } from "@/lib/roadmaps";

export function RoadmapHeader({ roadmap }: { roadmap: Roadmap }): JSX.Element {
  const current = roadmap.phases.find((phase) => phase.status === "active");
  const target = roadmap.goal_target_date ? new Intl.DateTimeFormat(undefined, { month: "short", year: "numeric" }).format(new Date(`${roadmap.goal_target_date}T00:00:00`)) : "Flexible";
  return <header className="sticky top-16 z-10 -mx-4 border-b border-slate-800 bg-slate-950/95 px-4 pb-5 pt-2 backdrop-blur sm:-mx-8 sm:px-8"><div className="mx-auto max-w-7xl"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-sky-400">Personalized roadmap</p><h1 className="mt-2 text-2xl font-bold sm:text-3xl">{roadmap.goal_title}</h1></div><span className="w-fit rounded-full border border-slate-700 px-3 py-1 text-xs text-slate-400">Target: {target}</span></div><div className="mt-5 grid grid-cols-2 gap-3 lg:grid-cols-4">{[["Total Phases", roadmap.total_phases], ["Estimated Time", `${roadmap.estimated_weeks ?? "—"} weeks`], ["Progress", `${Math.round(roadmap.overall_progress_percentage)}%`], ["Current Phase", current ? `Phase ${current.order_index + 1}` : "Complete"]].map(([label, value]) => <div key={label} className="rounded-xl border border-slate-800 bg-slate-900/70 p-3"><p className="text-xs text-slate-500">{label}</p><p className="mt-1 font-bold">{value}</p></div>)}</div><div className="relative mt-5 h-3 overflow-hidden rounded-full bg-slate-800"><div className="h-full bg-gradient-to-r from-emerald-400 to-sky-400 transition-all duration-500" style={{ width: `${roadmap.overall_progress_percentage}%` }} />{roadmap.phases.slice(1).map((phase) => <i key={phase.id} className="absolute inset-y-0 w-px bg-slate-950" style={{ left: `${(phase.order_index / roadmap.total_phases) * 100}%` }} />)}<i className="absolute top-1/2 h-3 w-3 -translate-x-1/2 -translate-y-1/2 animate-pulse rounded-full bg-white shadow shadow-sky-400" style={{ left: `${roadmap.overall_progress_percentage}%` }} /></div></div></header>;
}
