import type { LeaderboardEntry } from "@/lib/progress";

export function Leaderboard({ entries }: { entries: LeaderboardEntry[] }): JSX.Element {
  return <section className="overflow-hidden rounded-2xl border border-slate-800 bg-slate-900/60"><div className="p-6"><p className="text-xs font-bold uppercase tracking-wider text-amber-300">Community</p><h2 className="mt-2 text-xl font-bold">Top learners</h2></div><div className="overflow-x-auto"><table className="w-full min-w-[520px] text-sm"><thead className="bg-slate-950/60 text-left text-xs uppercase text-slate-500"><tr><th className="px-5 py-3">Rank</th><th className="px-5 py-3">Learner</th><th className="px-5 py-3">Level</th><th className="px-5 py-3">XP</th><th className="px-5 py-3">Streak</th></tr></thead><tbody className="divide-y divide-slate-800">{entries.map(entry=><tr key={`${entry.rank}-${entry.user_name}`} className={entry.is_current_user?"bg-sky-400/10":""}><td className="px-5 py-3 font-bold">{entry.rank<=3?["🥇","🥈","🥉"][entry.rank-1]:entry.rank}</td><td className="px-5 py-3">{entry.is_current_user?"You":entry.user_name}</td><td className="px-5 py-3">Lv {entry.level}</td><td className="px-5 py-3">{entry.total_xp.toLocaleString()}</td><td className="px-5 py-3">🔥 {entry.streak}</td></tr>)}</tbody></table></div></section>;
}
