"use client";
import type{Project}from"@/lib/projects";
export function ProjectDetail({project,eligible,recommendation,onStart,starting}:{project:Project;eligible:boolean;recommendation:string;onStart:()=>void;starting:boolean}):JSX.Element{return <div className="mx-auto max-w-5xl"><header className="rounded-2xl border border-indigo-400/20 bg-gradient-to-br from-indigo-400/10 to-slate-900 p-8"><p className="text-xs font-bold uppercase tracking-wider text-indigo-300">{project.category.replaceAll("-"," ")} · Difficulty {project.difficulty_level}</p><h1 className="mt-3 text-4xl font-black">{project.title}</h1><p className="mt-4 max-w-3xl leading-7 text-slate-300">{project.description}</p><div className="mt-5 flex flex-wrap gap-2">{project.tech_stack.map(item=><span key={item} className="rounded-full bg-slate-800 px-3 py-1 text-xs">{item}</span>)}</div><div className="mt-7 flex flex-wrap items-center gap-4"><button disabled={!eligible||starting} onClick={onStart} className="rounded-xl bg-sky-400 px-6 py-3 font-bold text-slate-950 disabled:cursor-not-allowed disabled:opacity-50">{starting?"Preparing workspace…":eligible?"Start Project →":"Prerequisites required"}</button><span className="text-sm text-slate-400">{project.estimated_hours} hours · {project.stages_count} stages</span></div><p className={`mt-3 text-sm ${eligible?"text-emerald-300":"text-amber-300"}`}>{recommendation}</p></header><div className="mt-8 grid gap-8 lg:grid-cols-[1fr_1.4fr]"><section><h2 className="text-xl font-bold">Learning outcomes</h2><ul className="mt-4 space-y-3">{project.learning_outcomes.map(item=><li key={item} className="flex gap-3 text-sm text-slate-300"><span className="text-emerald-300">✓</span>{item}</li>)}</ul></section><section><h2 className="text-xl font-bold">Project stages</h2><div className="mt-4 space-y-3">{project.stages.map(stage=><div key={stage.id} className="rounded-xl border border-slate-800 bg-slate-900/50 p-4"><div className="flex justify-between gap-3"><b>{stage.order_index+1}. {stage.title}</b><span className="text-xs text-slate-500">{stage.estimated_minutes} min</span></div><p className="mt-1 text-sm text-slate-400">{stage.description}</p></div>)}</div></section></div></div>}
