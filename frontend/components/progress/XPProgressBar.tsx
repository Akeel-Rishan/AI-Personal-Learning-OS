"use client";

import type { XPSummary } from "@/lib/progress";

export function XPProgressBar({ xp, compact = false }: { xp: XPSummary; compact?: boolean }): JSX.Element {
  return <section className={`${compact ? "p-4" : "p-6"} rounded-2xl border border-indigo-400/20 bg-gradient-to-r from-indigo-400/10 to-sky-400/5`}><div className="flex items-center gap-4"><span className={`${compact ? "h-10 w-10 text-sm" : "h-14 w-14 text-lg"} flex shrink-0 items-center justify-center rounded-full bg-gradient-to-br from-indigo-400 to-sky-400 font-black text-slate-950 shadow-lg shadow-indigo-950`}>{xp.level}</span><div className="min-w-0 flex-1"><div className="flex flex-wrap items-baseline justify-between gap-2"><p className="font-bold">Level {xp.level} — {xp.level_title}</p><span className="text-xs text-slate-400">{xp.total_xp.toLocaleString()} XP total</span></div><div className="mt-2 h-2.5 overflow-hidden rounded-full bg-slate-800"><div className="h-full rounded-full bg-gradient-to-r from-indigo-400 to-sky-400 transition-all duration-700" style={{ width: `${xp.progress_percentage}%` }} /></div><div className="mt-1.5 flex justify-between text-[11px] text-slate-500"><span>{xp.xp_progress} XP this level</span><span>{xp.xp_needed} XP to Level {Math.min(100, xp.level + 1)}</span></div></div></div>{!compact && xp.recent_xp_events.length > 0 && <div className="mt-5 grid gap-2 sm:grid-cols-2">{xp.recent_xp_events.slice(0, 4).map((event, index) => <div key={`${event.created_at}-${index}`} className="flex items-center justify-between rounded-lg bg-slate-950/40 px-3 py-2 text-xs"><span className="truncate text-slate-400">{event.description ?? event.event_type.replaceAll("_", " ")}</span><b className="ml-3 text-emerald-300">+{event.xp_earned}</b></div>)}</div>}</section>;
}
