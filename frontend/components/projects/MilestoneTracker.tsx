import type { UserProjectStage } from "@/lib/projects";
export function MilestoneTracker({stages}:{stages:UserProjectStage[]}):JSX.Element{const done=stages.filter(s=>s.status==="completed").length;return <section className="rounded-xl border border-slate-800 bg-slate-900/60 p-4"><div className="flex justify-between text-xs"><b>Milestones</b><span>{done}/{stages.length}</span></div><div className="mt-3 flex items-center">{stages.map((s,i)=><div key={s.id} className="flex flex-1 items-center"><span title={s.stage.title} className={`h-3 w-3 shrink-0 rounded-full ${s.status==="completed"?"bg-emerald-400":s.status==="active"||s.status==="submitted"?"bg-sky-400":"bg-slate-700"}`}/>{i<stages.length-1&&<span className={`h-0.5 flex-1 ${s.status==="completed"?"bg-emerald-400/60":"bg-slate-700"}`}/>}</div>)}</div><p className="mt-3 text-xs text-slate-500">{Math.round(done/Math.max(1,stages.length)*100)}% complete</p></section>}
