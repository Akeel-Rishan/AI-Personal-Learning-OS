import type { ProgressSummary as Summary } from "@/lib/progress";

export function ProgressSummary({ summary }: { summary: Summary }): JSX.Element {
  const cards = [[summary.total_skills_tracked, "Skills tracked", "◈"], [summary.skills_mastered, "Skills mastered", "✓"], [`${Math.round(summary.average_mastery * 100)}%`, "Average mastery", "↗"], [`${Math.round(summary.total_study_minutes / 60)}h`, "Study time", "◷"], [summary.total_exercises_completed, "Exercises done", "</>"], [`${summary.current_streak} days`, "Current streak", "🔥"]];
  return <section><div className="grid grid-cols-2 gap-3 lg:grid-cols-3 xl:grid-cols-6">{cards.map(([value, label, icon]) => <article key={label} className="rounded-2xl border border-slate-800 bg-slate-900/60 p-4"><span className="text-sky-300">{icon}</span><p className="mt-3 text-2xl font-black">{value}</p><p className="mt-1 text-xs text-slate-500">{label}</p></article>)}</div>{(summary.strongest_skill || summary.weakest_skill) && <div className="mt-4 grid gap-3 sm:grid-cols-2">{summary.strongest_skill && <p className="rounded-xl bg-emerald-400/5 p-4 text-sm text-emerald-200">Strongest: <b>{summary.strongest_skill.name}</b> · {Math.round(summary.strongest_skill.mastery * 100)}%</p>}{summary.weakest_skill && <p className="rounded-xl bg-amber-400/5 p-4 text-sm text-amber-200">Focus area: <b>{summary.weakest_skill.name}</b> · {Math.round(summary.weakest_skill.mastery * 100)}%</p>}</div>}</section>;
}
